# Web (React) placeholder

ADR-0001 expects a React frontend. After cloning the scaffold:

1. Initialize Vite + React in this folder (or replace it).
2. Point API calls at the OpenAPI contract in `<projectDir>/Docs/openapi.yaml`.
3. Keep env-specific hosts out of source (use `.env` / config).

Do not treat this stub as a finished UI.
